{
  "name": "flexdev-site",
  "private": true,
  "scripts": {
    "build": "tailwindcss -c tailwind.config.js -i input.css -o ../assets/tailwind.css --minify",
    "watch": "tailwindcss -c tailwind.config.js -i input.css -o ../assets/tailwind.css --watch"
  },
  "devDependencies": { "tailwindcss": "^3.4.17" }
}
